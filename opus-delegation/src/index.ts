/**
 * Opus Delegation System
 *
 * A toolchain for delegating complex architectural problems to Claude Opus 4.5
 * via use.ai by automating context extraction, delegation request formatting,
 * artifact parsing, and implementation guide generation.
 */

export * from './types';
