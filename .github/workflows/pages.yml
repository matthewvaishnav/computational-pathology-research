name: Deploy GitHub Pages Legacy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment ownership
        run: echo "Legacy Docusaurus deployment is disabled. MkDocs deploys the site via .github/workflows/mkdocs.yml."
