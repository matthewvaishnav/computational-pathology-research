name: Deploy GitHub Pages Legacy Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment ownership
        run: echo "Legacy Docusaurus Pages deployment is disabled. The canonical public manuscript site is published by .github/workflows/publish-panf-manuscript.yml."
