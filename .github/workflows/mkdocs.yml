name: Deploy MkDocs Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment ownership
        run: echo "Legacy MkDocs deployment is disabled. The canonical public manuscript site is published by .github/workflows/publish-panf-manuscript.yml."
