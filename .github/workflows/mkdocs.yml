name: Deploy MkDocs Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment ownership
        run: echo "MkDocs deployment is disabled. The Deploy VitePress workflow now publishes the paper PDF site."
