# Example Secret manifest for historical HistoCore streaming scaffolding.
# No secret material is stored in the repository.
#
# Populate this object at deployment time from an external secret manager or a
# local, untracked manifest. This file is research/development infrastructure,
# not evidence of an active deployment.
apiVersion: v1
kind: Secret
metadata:
  name: histocore-secret
  namespace: histocore
type: Opaque
stringData: {}
