# FAIR-WEIGHTS-H Theory

FAIR-WEIGHTS-H models institutional influence as entropy-regularized utility allocation.

Implemented default:

w_i = exp(z_i) / sum_j exp(z_j)

Experimental extensions:
- temperature parameter beta
- log-linear scoring
- mirror descent / multiplicative weights

This theory is experimental and requires empirical validation.
