# Scientific audit remediation — 2026-07-25

## Purpose

This ledger records which scientific problems were corrected in code, which
historical claims were withdrawn, and which experiments must be rerun before new
numbers can be promoted.

A code correction does not retroactively validate an artifact generated by the
old code.

## Corrected in this branch

### TransnnMIL

- Replaced the one-query/one-key fusion path with self-attention over explicit
  branch tokens.
- Both TransMIL and nnMIL branch representations can now affect the fused output.
- Moved the topology projection into `__init__` as a registered trainable module.
- Retained the old gate parameter only for checkpoint/API compatibility and
  explicitly stopped interpreting it as branch importance.
- Added tests requiring output sensitivity and gradient flow to both branches.
- Kept concat, gate, and learned branch-attention implementations as controlled
  fusion alternatives.

### Canine biological-label preservation audit

- Added a v2 runner using fit-only standardization for every linear probe.
- Corrected the scanner-centroid linear baseline to fit its scaler on fit rows
  only.
- Restricted category-neighbour retrieval to the fit pool.
- Excluded same-region and same-biological-sample candidates.
- Added fail-closed biological-sample leakage and category-support checks.
- Added biological-sample cluster-bootstrap intervals.
- Marked the historical audit report as superseded.

### SCORPION analysis and controls

- Added fold-aware two-stage fold/slide bootstrap inference.
- Removed the need to interpret slide-level sign-flip p-values as exact
  independent-unit evidence.
- Added frozen capacity-matched objective ablations, including a two-branch model
  with no scanner objectives and removal of each principal scanner-separation
  term.

### PCam

- Removed unsupported cross-paper leaderboard and significance claims.
- Removed claims about diagnoses saved, lives saved, clinical benefit, clinical
  readiness, acceptable workflow burden, or deployment.
- Reframed threshold values as retrospective test-set operating-point
  illustrations requiring validation-selected confirmation.

### Cross-experiment reporting

- Withdrew the unified scoreboard as a ranking device.
- Restricted future comparisons to within-protocol contrasts using matched data,
  preprocessing, probes, folds, seeds, and uncertainty procedures.

## Historical evidence withdrawn pending rerun

The following are not current claim evidence:

1. Historical TransnnMIL fusion and topology results generated by the defective
   execution paths.
2. Canine category-probe, category-purity, category/scanner-ratio, and linear
   baseline conclusions from the original biological-label audit.
3. Any cross-experiment ranking derived from the historical unified scoreboard.
4. Slide-independent SCORPION sign-flip p-values as exact inferential evidence.
5. PCam claims of clinical benefit or superiority over published models.

## Required reruns

### 1. TransnnMIL controlled rerun

Run canonical repaired fusion, concat, gate, and learned branch-attention variants
under identical PANDA splits, feature bags, optimization budgets, seeds, and
checkpoint-selection rules. Report branch ablations and matched compute.

Historical QWK values remain predictive outputs of the old defective model and
must not be relabelled as repaired-fusion evidence.

### 2. Canine label audit v2

```bash
python experiments/paired_acquisition/run_biological_label_preservation_audit_v2.py
```

Publish a new result directory only if the runner completes without missing or
non-finite required metrics.

### 3. SCORPION fold-aware inference

Run `scripts/scorpion/analyze_pathoalign_crossfold_v2.py` against the existing
`raw_slide_metrics.csv`. Treat the resulting intervals as the current inference
record and retain the five-fold small-cluster limitation.

### 4. SCORPION capacity-matched ablations

Run:

```bash
python experiments/scorpion/run_pathoalign_capacity_matched_ablations.py \
  --base-features <frozen-feature-archive> \
  --manifests-dir data/scorpion/splits \
  --out-dir <new-output-directory>
```

The frozen design contains seven variants, five folds, and five seeds: 175 fits.
Analyze scanner suppression, acquisition capture, cross-view agreement, retrieval,
rank, and leakage without selecting variants on test outcomes.

### 5. Forward-valid releases

New numerical claims from the reruns must be published through the forward-valid
provenance contract. Historical NPZ files with unresolved lineage remain
excluded.

## Remaining limitations not solvable by a code patch

- SCORPION has no biological-category labels.
- Canine SCC is currently the only labeled multi-scanner category benchmark.
- A second labeled multi-scanner dataset is needed for category-level external
  confirmation.
- Independent human-tissue external validation remains necessary.
- PANDA patient-disjoint evaluation requires trustworthy patient mapping.
- CAMELYON17 provides only one held-out test institution for the current
  center-weighting case study.
- Clinical, diagnostic, patient-outcome, and deployment claims remain forbidden.

## Current defensible center of gravity

The strongest current claim remains a bounded representation result:

> Under the tested paired-acquisition protocols, the learned biological branch
> contains substantially less linearly recoverable scanner identity, while an
> explicit acquisition branch retains scanner information and same-region tissue
> identity is largely preserved.

This is evidence of partial structured separation under tested conditions. It is
not proof of pure biological factors, complete independence, universal scanner
invariance, clinical utility, or diagnostic improvement.
